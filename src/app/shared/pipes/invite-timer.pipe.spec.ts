import { InviteTimerPipe } from './invite-timer.pipe';

describe('InviteTimerPipe', () => {
  it('create an instance', () => {
    const pipe = new InviteTimerPipe();
    expect(pipe).toBeTruthy();
  });
});
